[pytest]
# Fast, model-free suite by default. Heavy tests (spaCy / HuggingFace model
# loads) run with:  python -m pytest tests/ -m heavy
addopts = -m "not heavy"
markers =
    heavy: needs spaCy/HuggingFace models (run with: pytest -m heavy)
testpaths = tests
